<UserControl x:Class="Presentation.View.ProductMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Presentation.View"
             xmlns:vm="clr-namespace:Presentation.ViewModel"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:ProductListViewModel/>
    </UserControl.DataContext>

    <DockPanel Margin="10">
        <TextBlock FontSize="24" FontWeight="Bold" DockPanel.Dock="Top" Text="Products" Margin="0 0 0 10"/>

        <!-- Sekcja szczegółów -->
        <GroupBox Header="Selected Product" DockPanel.Dock="Top" Margin="0 0 0 10">
            <local:ProductDetailView DataContext="{Binding SelectedProduct}" />
        </GroupBox>

        <!-- Przycisk + lista -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0 0 0 10">
                <Button Content="Add" Command="{Binding AddProductCommand}" Width="80" Margin="0 0 10 0"/>
                <Button Content="Delete" Command="{Binding DeleteProductCommand}" Width="80" Margin="0 0 10 0"/>
                <Button Content="Save" Command="{Binding SaveCommand}" Width="80"/>
            </StackPanel>

            <!-- Lista produktów -->
            <ListView Grid.Row="1"
                      ItemsSource="{Binding Products}"
                      SelectedItem="{Binding SelectedProduct}"
                      Margin="0"
                      SelectionMode="Single">
                <ListView.View>
                    <GridView>
						<GridViewColumn Header="Id" DisplayMemberBinding="{Binding id}" Width="100"/>
						<GridViewColumn Header="Name" DisplayMemberBinding="{Binding name}" Width="150"/>
						<GridViewColumn Header="Price" DisplayMemberBinding="{Binding price}" Width="100"/>
						<GridViewColumn Header="Quantity" DisplayMemberBinding="{Binding quantity}" Width="100"/>
						<GridViewColumn Header="Description" DisplayMemberBinding="{Binding description}" Width="200"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DockPanel>
</UserControl>
