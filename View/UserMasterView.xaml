<UserControl x:Class="View.UserMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:View"
             xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
             Height="Auto" Width="Auto">
    <UserControl.DataContext>
        <vm:UserListViewModel/>
    </UserControl.DataContext>

    <DockPanel Margin="20">
        <TextBlock FontSize="24" FontWeight="Bold" DockPanel.Dock="Top" Text="Users" Margin="0,0,0,10"/>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <GroupBox Header="Selected user" Grid.Row="0" Margin="0,0,0,10">
                <local:UserDetailView DataContext="{Binding SelectedViewModel}" />
            </GroupBox>

            <GroupBox Header="Users list" Grid.Row="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <ListView ItemsSource="{Binding Users}"
                              SelectedItem="{Binding SelectedViewModel}"
                              Grid.Row="0"
                              Margin="0,0,0,5">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Username" DisplayMemberBinding="{Binding Username}" Width="150"/>
                                <GridViewColumn Header="Password" DisplayMemberBinding="{Binding Password}" Width="150"/>
                                <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="150"/>
                                <GridViewColumn Header="Phone" DisplayMemberBinding="{Binding PhoneNumber}" Width="150"/>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" Margin="5">
                        <Button Content="Add" Command="{Binding AddCommand}" Margin="5"/>
                        <Button Content="Remove" Command="{Binding DeleteCommand}" Margin="5"/>
                        <Button Content="Update" Command="{Binding UpdateCommand}" Margin="5"/>
                        <Button Content="Load" Command="{Binding LoadCommand}" Margin="5"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>
    </DockPanel>
</UserControl>
