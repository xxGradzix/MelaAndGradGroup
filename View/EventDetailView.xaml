<UserControl x:Class="Presentation.View.EventDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:EventViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Event details" FontSize="18" FontWeight="Bold" Margin="0 0 0 10"/>

            <TextBlock Text="Event ID:" FontWeight="SemiBold"/>
            <TextBox Text="{Binding EventId}" IsReadOnly="True" Margin="0 0 0 10"/>

            <TextBlock Text="NrOfProducts:" FontWeight="SemiBold"/>
            <TextBox Text="{Binding NrOfProducts}" IsReadOnly="True"/>
        </StackPanel>
    </Grid>
</UserControl>
